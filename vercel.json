{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "ignoreCommand": "git diff --quiet HEAD~1 HEAD -- ':!docs'",
  "crons": [
    {
      "path": "/api/cron/agents",
      "schedule": "0 6 * * *"
    }
  ]
}
